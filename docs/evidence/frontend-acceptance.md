# 管理前端本機驗收（2026-09-21 至 2026-09-22）

範圍：React 管理工作台與現有 FastAPI/PostgreSQL 功能。平台登記外部 Agent 身份，不執行 Agent。全程使用獨立驗收團隊，未把驗收會話加入內部團隊。

## 自動檢查

- PostgreSQL 正式遷移後測試：43 passed、1 skipped（真實模型需顯式開啟），2 個依賴棄用警告。證據：`frontend-suite.xml`。
- 前端 Vitest：5 passed。涵蓋機器憑證與 Cookie 分離、衝突／網路錯誤、驗證訊息，以及登入觀察器在清除工作階段後仍可更新。
- TypeScript + Vite 正式建置通過。主 JS 約 1.22 MB（gzip 約 383 KB），仍有 chunk 大小警告；尚未做載入效能驗收。
- 生產 npm 依賴 audit：0 vulnerabilities（當次檢查結果，不代表永久安全）。
- Ruff：src、tests、本次新 migration 通過。擴大至既有 migration 會看到先前產生的 import/type style 提示，未在此次修改舊 migration。
- Alembic check：No new upgrade operations detected。

## 真實瀏覽器與後端

已操作並觀察結果：

1. 未登入 → 登入工作台；建立知識空間後重新載入仍存在。
2. 登記外部 Agent → 建立服務對象；以獨立測試憑證驗證身份與唯一 Subject 範圍。
3. 建立會話 → 錄入一則虛構偏好訊息 → 提交真實 metis-coder 提取。
4. 任務 `25d6f9ea2eda4aa1aa540310d01ccfdf` 前兩次為 `extraction_invalid_response`，透過 UI 明確重試，第三次成功產生兩條候選。沒有自動偽造候選或把失敗顯示為成功。診斷另有一次同輸入真實模型呼叫，JSON 可解析；未證明前兩次錯誤的具體格式或來源根因。
5. 候選來源抽屜顯示原始訊息與對應 ID；審核後記憶為待發布。
6. 修正內容後版本由 v1 增至 v2，歷史保留原內容、修正原因與來源。
7. 拒絕另一候選後，審計展開行顯示輸入的拒絕原因。
8. 任務中心顯示已完成與累計嘗試 3 次；終態停止輪詢。
9. 切換至另一個僅檢視者團隊，頁面無前一團隊空間，導航只顯示知識空間／團隊成員。
10. 桌面 1440×900 與窄視窗實際檢查。修正窄視窗選單遮住團隊選擇器的问题；導航改為浮層並在選取後收起。瀏覽器未觀察到 console error。

## 此次修正

- 清除所有 QueryCache 會使既有 profile observer 留在 pending，首次登入頁一直轉圈；改為保留 profile query、取消請求、移除其他資料並置空身份。加入回歸測試。
- 同一憑證驗證失敗後再次提交，現在會明確 refetch；相同任務 ID 查詢也可重試。
- 候選拒絕原因保存至審計；新增持久化斷言與來源權限檢查。
- 任務詳情隨列表更新完整資料，避免只更新狀態而保留舊 token 使用量。

## 邊界

這是本機功能驗收，尚非整套 V1 生產交付。文件匯入、引擎發布與刪除、檢索/context、向量模型、完整恢復、容量與運維驗收仍未完成。真實模型的輸出穩定性需再處理。記憶刪除/停用、授權與憑證輪替的主要證據來自後端測試，未把每種分支都宣稱為瀏覽器實測。
